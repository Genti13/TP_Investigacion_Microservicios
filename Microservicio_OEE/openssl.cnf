openssl_conf = default_conf

[default_conf]
ssl_conf = ssl_sect

[ssl_sect]
system_default = ssl_default_sect

[ssl_default_sect]
# Permitir protocolos TLS antiguos
MinProtocol = TLSv1
# Desactivar restricciones de seguridad (SECLEVEL=0)
CipherString = DEFAULT:@SECLEVEL=0